services:
  - type: web
    name: tg-wa-bridge
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    autoDeploy: true
    envVars:
      - key: PORT
        value: 3000
    healthCheckPath: /healthz
